# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Database — SQLite for dev, PostgreSQL for production
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
aiosqlite==0.20.0
alembic==1.13.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Config
pydantic-settings==2.5.2
python-dotenv==1.0.1

# AI / Embeddings
openai==1.51.0
numpy==1.26.4

# Resume parsing
pdfplumber==0.11.4
python-docx==1.1.2

# HTTP client (for API-based scrapers: Greenhouse, Lever, Arbeitnow, RemoteOK)
httpx==0.27.2

# Playwright (for JS-heavy scrapers: Naukri, LinkedIn, Workday)
playwright==1.47.0

# NOTE: The following are optional heavy deps not installed on Render free tier:
# faiss-cpu   — large C++ lib. Removed; similarity search uses numpy fallback.
# celery      — task queue for async scraping. Not needed for API-based scrapers.
# redis       — celery broker. Not needed without celery.
# sendgrid    — email service. Optional.

# Testing (dev only — not needed in production but kept for CI)
pytest==8.3.3
pytest-asyncio==0.24.0
